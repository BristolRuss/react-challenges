import React from 'react';

const properties = {
    background: "red", 
    width: "200px",
    height: "200px"
};

const Square = () => (
    <div style = {properties}></div>
);

export default Square;